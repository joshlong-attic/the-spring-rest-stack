<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jl.crm.android"
    android:versionCode="1"
    android:versionName="1.0">

    <!--
    TODO add android:excludeFromRecents="true" android:noHistory="true" to each <activity>
    -->
    <uses-sdk
        android:minSdkVersion="17"
        android:targetSdkVersion="17" />
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:logo="@drawable/spring_crm_logo"
        android:icon="@drawable/spring_crm_logo"
        android:name=".Crm"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Holo.Light.DarkActionBar">

        <activity android:name=".activities.AuthenticationActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="x-com-jl-crm-android" android:host="crm-oauth-response" />
            </intent-filter>
        </activity>

        <!-- Support Searching our Customer records
        -->
        <activity android:name=".activities.CustomerSearchActivity" >
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>

        <!--
        <provider android:name="DictionaryProvider"
            android:authorities="dictionary"
            android:syncable="false" />-->

        <activity android:name=".activities.UserHomeActivity" />

        <!-- <activity android:name=".SearchableDictionary"
             android:launchMode="singleTop">

             <intent-filter>
                 <action android:name="android.intent.action.MAIN" />
                 <category android:name="android.intent.category.LAUNCHER" />
             </intent-filter>

             &lt;!&ndash; Receives the search request. &ndash;&gt;
             <intent-filter>
                 <action android:name="android.intent.action.SEARCH" />
                 &lt;!&ndash; No category needed, because the Intent will specify this class component&ndash;&gt;
             </intent-filter>

             &lt;!&ndash; Points to searchable meta data. &ndash;&gt;
             <meta-data android:name="android.app.searchable"
                 android:resource="@xml/searchable" />

         </activity>

         &lt;!&ndash; Displays the definition of a word. &ndash;&gt;
         <activity android:name=".WordActivity" />

         &lt;!&ndash; Provides search suggestions for words and their definitions. &ndash;&gt;
         <provider android:name=".DictionaryProvider"
             android:authorities="com.example.android.searchabledict.DictionaryProvider" />

        -->


        <!--<meta-data android:name="android.app.default_searchable" android:value=".activities.CustomerSearchActivity" />-->

    </application>

</manifest>